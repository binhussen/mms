import returnWeaponForm from './return-weapon.form';

const returnWeaponDetailPage = {
  title: 'Request For Return Weapon',
  type: 'default page',
  form: returnWeaponForm.requestForReturningWeaponItemsForm,
  table: returnWeaponForm.requestForReturningWeaponItemsForm,
};

export default returnWeaponDetailPage;
