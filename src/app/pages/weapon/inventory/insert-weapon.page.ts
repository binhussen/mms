import inventoryForm from './inventory.form';
import inventoryTableState from './inventory.table';

const insertWeaponPage = {
  title: 'Insert Weapon',
  type: 'default page',
  form: inventoryForm.inventoryForm,
  table: inventoryTableState.inventoryTableState,
};

export default insertWeaponPage;
